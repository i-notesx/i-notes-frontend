{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose-overide.yml up -w",
    "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose-prod.yml restart",
    "docker:stop": "docker-compose down"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.5"
  }
}
